name: Build and Release

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'

      - name: Set HBWPath environment variable
        run: |
          echo HBWPath=src/Stripped >> $GITHUB_ENV

      - name: Restore dependencies
        run: dotnet restore src/HBWAbilitySwitcher.csproj

      - name: Build project
        run: dotnet build src/HBWAbilitySwitcher.csproj --configuration Release

      - name: Create output directory
        run: |
          if not exist output mkdir output

      - name: Copy build artifacts
        run: |
          xcopy /Y /I src/bin/Debug/netstandard2.1/* output

      - name: Copy additional files
        run: |
          xcopy /Y /I src/Stripped/* output

      - name: Zip all into HBWAbilitySwitcher folder
        run: |
          mkdir HBWAbilitySwitcher
          xcopy /Y /I output/* HBWAbilitySwitcher
          powershell Compress-Archive -Path HBWAbilitySwitcher/* -DestinationPath HBWAbilitySwitcher.zip

      - name: Upload release artifact
        uses: actions/upload-artifact@v3
        with:
          name: HBWAbilitySwitcher
          path: HBWAbilitySwitcher.zip

      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ github.run_number }}
          release_name: Release v${{ github.run_number }}
          draft: false
          prerelease: false
          files: HBWAbilitySwitcher.zip
